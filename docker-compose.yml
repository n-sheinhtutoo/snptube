services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DOWNLOAD_DIR=/app/downloads
      - LOG_LEVEL=INFO
      - CORS_ORIGINS=*
    volumes:
      - downloads:/app/downloads
    restart: unless-stopped

volumes:
  downloads:
